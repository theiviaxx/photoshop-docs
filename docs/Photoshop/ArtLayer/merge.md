ArtLayer.merge {#ArtLayer.merge}
==============

> [ArtLayer]{role="ref"} **merge** ()

Description
-----------

Merges the layer down, removing the layer from the document. Returns a
reference to the art layer that this layer is merged into.
